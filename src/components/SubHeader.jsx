import React from 'react';
import styled from 'styled-components';

const Header = styled.div `
    font-family: 'IndustrialRegular'; 
`

const subHeader = () => {
    return (
        <Header>
            Collectible kicks for the digital age
        </Header>
    )
}

export default subHeader
